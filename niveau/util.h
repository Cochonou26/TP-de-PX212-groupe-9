// Taille maximale de la chaine représentant le niveau (4 caractères + '\0')
#define MAXSTRNIVEAU 5 
// Taille maximale d'une ligne dans le fichier contenant les niveaux
#define MAXSIZE 100
// Macro permettant de trouver le max de deux nombres
#define MAX(a,b) (a>b?a:b)
